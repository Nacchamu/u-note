%div.mypage-left
  %div.mypage-left__cover

  %ul.mypage-left__menues
    %li 閲覧数の解析
    = link_to mypages_favorite_path do
      %li お気に入り記事
    = link_to edit_user_registration_path do
      %li プロフィール設定
    = link_to '/users', method: :delete do
      %p.mypage-left__menues__delete-button >アカウントを削除する

%div.mypage-user-info
  - @favorites.each do |favorite|
    %div.article
      = link_to note_path(favorite.id) do
        %img.article__image{src: favorite.article.thumbnail }
      %div.article__texts
        = link_to note_path(favorite.article.id) do
          %p.article__texts__title= favorite.article.title
        %div.article__texts__detail
          != favorite.article.text
        %p.article__texts__user-name
        %p.article__texts__date= favorite.article.created_at.strftime('%Y年%m月%d日 %H時%M分')